# 設計文件 · 群組類型：哪些群定義組織，哪些不定義（M0）

> 狀態：✅ **M0 APPROVED v0.2**（2026-08-21）· **OQ-GTC-1..10 全採建議** · 待客戶回答 C1–C4 · 下一步 M0.5
> UI mockup：[`../mockup/group-type-org-after.html`](../mockup/group-type-org-after.html)（待 review）
> 對象：`line_group`、`org-overview.service.ts`、部門健康度／簽核率分母、`ticket-materializer`
> 相關：[`org-overview.md`](org-overview.md)、[`member-department-assignment.md`](member-department-assignment.md)、
> [`warroom-task-board.md`](warroom-task-board.md)、[`design-research-org-chart-layout.md`](design-research-org-chart-layout.md)
>
> **一句話**：66% 的員工「跨部門」，但那不是台灣福祉的組織形狀 ——
> 是我們把**每一個 LINE 群都當成一個部門**，而其中有全員群、流程群、甚至測試群。
> 問題出在分類，就該在分類上解，不是把 `users` 改成多對多。

---

## 1. 觸發事件（2026-08-21 · 對廠商展示）

用戶展示組織圖時，廠商當場指出「**一位員工跨多個部門**」。

初步判斷有三種可能（A 一人身兼兩職／B 一人在多個 LINE 群／C 主管要看多部門），
用戶確認是 **B**。B 的好消息是資料層本來就記得住（`line_member` 唯一鍵是
`(bot_id, group_id, user_id)`，一人多列很正常），壞消息是查下去發現**根因不是 B**。

---

## 2. 現況走查（prod 實測 · 台灣福祉）

### 2.1 跨部門是多數，不是例外

| 跨幾個部門 | 幾人 |
|---:|---:|
| 1 | 19 |
| 2 | **20** |
| 3 | 8 |
| 4 | 3 |
| 5 | 5 |
| 9 | 1 |

**37 / 56 ＝ 66% 跨部門。** 在這個比例下，「一人一部門」不是簡化，是錯的。

### 2.2 但「有你真好」解釋掉一半

各群人數（已排除 `fetch_error`）：

| 群組 | 已分派部門 | 人數 |
|---|:--:|--:|
| **有你真好** | t | **40** |
| **報工及車輛調度** | t | **19** |
| 福祉集團-業務部 | t | 15 |
| 福祉集團-業務二部(含售服) | t | 13 |
| 技術工程部組長/生管/物控 | t | 11 |
| 技術工程部_技術開發組 | t | 10 |
| 福祉業務助理小組 | t | 7 |
| 福祉集團-人資總務群 | t | 6 |
| 福祉—品保部 | t | 5 |
| **台灣福祉機器人測試群** | **t** ⚠️ | 4 |
| 福祉 分析表測試群 | f | 4 |
| 台灣福祉法規組 | t | 3 |
| AI-TWBRAUN, 柏淵 | f | 2 |
| 福祉 Line 通知測試群 | f | 2 |
| 福祉LIFF | f | 1 |
| 鮮湧, 柏淵 | f | 1 |

「有你真好」**40 人 ＝ 全部 56 人的 71%**，是第二名的兩倍多 —— 這是**全員群**，不是部門。
它被分派成部門之後，**每個在裡面的人都自動「跨」了一個部門**。

排除它重算：**21 / 46 ＝ 46%**（總人數 56→46，表示**有 10 人只出現在全員群**）。

> ⚠️ 誠實記一筆：M0 撰寫者預測排除後會掉到「15 上下」，實際 21。方向對、量估低了。

### 2.3 第二個嫌疑犯（**未驗證**）

「報工及車輛調度」19 人，第二大群，且在跨群清單裡反覆與「技術工程部組長/生管/物控」
同時出現在同一人身上。名字本身是**一件事**而不是一個組織單位 ——
各部門的技師都要在那裡報工、排車。

**假設**：它是流程群。**尚未跑數字驗證**（排除它之後跨部門會掉到多少）。
進 M1 前應補跑（OQ-GTC-2）。

### 2.4 一個資料衛生問題

「**台灣福祉機器人測試群**」的 `department_id` **不是 NULL** —— 一個測試群被分派到了某個真實部門。
其他測試群與兩個一對一聊天（`AI-TWBRAUN, 柏淵`／`鮮湧, 柏淵`）都是未分派，只有這個漏網。

> ⚠️ **v0.2 更正**：v0.1 寫「4 個人跟著算進該部門的人數與健康度」，**那句沒有查證就寫了**。
> 組織圖的查詢有 `WHERE lg.status = 'active'`（`org-overview.service.ts:49`），
> 若該群不是 active 就根本不會被算進去。
> **實際有沒有汙染，取決於它的 `status`，M0.5 要查**（OQ-GTC-2 同批）。
> 但無論如何「測試群不該掛在真部門下」這件事本身仍要修。

### 2.5 更大的落差：56 人 vs 12 個帳號

| | 人數 |
|---|--:|
| 在 LINE 群裡講過話的人 | **56** |
| 有系統帳號（`users`）的人 | **12** |
| 組織圖畫得出來的成員 | **11** |

組織圖畫的是 `users`，現場實際在的是 `line_member` —— **兩群不同的人**。
台灣福祉的人心裡的公司有五六十人，畫面上只有 11 個。

⭐ 這跟 [`task-assign-notify.md`](task-assign-notify.md) 卡住的是**同一件事**：
名單裡的「☜我是佳慧🍭佳慧是我☞」正是當時 AI 抽到、系統裡查無此人的那個佳慧。
本文把它記下來，但**是否納入本模組待裁定**（OQ-GTC-9）。

---

## 3. 根因

**LINE 群至少有三種語意，而我們只有一種模型。**

| 種類 | 例子 | 語意 | 該定義組織嗎 |
|---|---|---|:--:|
| **部門群** | 福祉集團-業務部、台灣福祉法規組、福祉—品保部 | 一個組織單位 | ✅ |
| **流程／主題群** | 有你真好（全員公告）、報工及車輛調度（跨部門作業） | 一件事，參與者橫跨部門 | ❌ |
| **測試／私訊** | 福祉 分析表測試群、`AI-TWBRAUN, 柏淵` | 不是組織的一部分 | ❌ |

`line_group` 目前只有 `department_id` 一個欄位表達歸屬，於是三種都被塞進同一個結構。
結果就是「每個人都跨部門」—— **那不是組織的形狀，是我們的分類把它變成那樣。**

---

## 4. 設計

### 4.1 ⚠️ 一個原本想做、但被 schema 否決的方向

原本的直覺是「流程群不掛部門」（`department_id = NULL`）。**不行**：

```sql
-- 0001_init.sql:57
tickets.department_id uuid NOT NULL REFERENCES departments(...)
```

`tickets.department_id` 是 **NOT NULL**，而**流程群正是產出最多任務的地方**
（報工群 19 人、天天在報工）。拿掉部門關聯 → 材料化建卡直接失敗 → 那些任務**整批消失**。

> 這是為什麼要先讀 schema 再寫設計。少看這一行，M1 會在 prod 上炸。

### 4.2 正解：類型是「顯示與統計」的分類，不是「移除關聯」

`line_group` **加一個 `group_type`，`department_id` 一律保留**。

| 用途 | 讀什麼 | 流程群算不算 |
|---|---|:--:|
| 任務要掛在哪個部門（材料化） | `department_id`（照舊）| ✅ 算 |
| 組織圖的成員歸屬 | `group_type = 'department'` 才算 | ❌ 不算 |
| 部門健康度／簽核率的**分母** | 同上 | ❌ 不算 |
| 訊息分析、任務產出 | 照舊全部都跑 | ✅ 算 |

⭐ **關鍵取捨：流程群的任務仍然有部門、仍然要簽核、仍然進看板** ——
只是它**不再宣稱「這些人屬於那個部門」**。
把「資料歸屬」與「組織歸屬」拆開，是這份設計的全部內容。

### 4.3 資料模型（migration 0066）

```sql
ALTER TABLE line_group
  ADD COLUMN IF NOT EXISTS group_type text NOT NULL DEFAULT 'department'
    CHECK (group_type IN ('department','process','announcement','test'));
```

- **預設 `'department'`** —— 既有 13 個群行為完全不變（R1：破壞性變更要可回退）
- 回填由人判斷，不自動猜（OQ-GTC-3）

### 4.4 UI

- **通訊管道 → LINE 群組**：每個群多一個「群組類型」下拉，附一句說明
  （「流程群：跨部門協作用，成員不會被視為該部門的人」）
- **組織圖**：只吃 `department` 型的群；流程群另開一區「跨部門群組」，
  列出群名 + 人數，**不畫進部門樹**
- **部門健康度**：分母說明加一句「不含跨部門群組」

---

## 5. 企業級 cross-cutting

### 5.1 數字會跳動 —— 這是最大的溝通風險
改完之後部門人數、健康度、簽核率**都會變**（分母縮小）。
客戶看到數字一夜之間不一樣，第一反應是「系統壞了」。
→ 必須有**變更當下的對照說明**（改前／改後、為什麼），不能靜靜地換掉（OQ-GTC-7）。

### 5.2 觀測
`group_type` 的每次變更要進 `audit_log`（R5）—— 它會改變統計口徑，
事後追「那天數字為什麼變」需要這條線索。

### 5.3 容量與成本
一個欄位、一次回填，無新增查詢成本。組織圖少畫幾個節點反而更快。

### 5.4 向後兼容
`DEFAULT 'department'` ＝ 不回填就行為不變。
材料化、RLS、簽核流程**一行都不用改** —— 這是 §4.2 那個取捨換來的。

---

## 6. 待客戶確認

| # | 問題 | 為什麼要問 |
|---:|---|---|
| **C1** | 「有你真好」是什麼群？（全員／社交／公告）| 40 人 ＝ 71%，幾乎確定不是部門，但要客戶自己說 |
| **C2** | 「報工及車輛調度」是部門還是跨部門的作業群？ | §2.3 的假設，決定第二個回填 |
| **C3** | 13 個群裡還有哪些其實不是部門？ | 回填要人判斷，不能猜 |
| **C4** | 「舊資料-待刪除」那個部門要刪嗎？ | 4 人掛在那裡，佔著人數與健康度 |

---

## 7. 落地順序

| 里程碑 | 內容 | 前置 |
|---|---|---|
| **M0** | 本文 · 待裁定 OQ · 待客戶回答 C1–C4 | — |
| **M0.5** | 補跑「排除報工及車輛調度」的數字（§2.3）· 修「台灣福祉機器人測試群」的錯誤分派（§2.4）| — |
| **M1** | migration 0066 加 `group_type` ＋ 回填（人工判斷）| C1–C3 |
| **M2** | 組織圖只吃 `department` 型 ＋ 另開「跨部門群組」區 | M1 |
| **M3** | 部門健康度／簽核率分母排除非部門群 ＋ 變更對照說明 | M2 |
| **M4** | 通訊管道頁的類型下拉（讓客戶自己維護）| M2 |
| **M5** | FMEA 覆核（R17）＋ MODULES.md | — |

---

## 8. 開放問題（OQ-GTC-N）— ✅ 2026-08-21 全採建議

> 下欄「建議」即為裁定結果。落地形狀見
> [`../mockup/group-type-org-after.html`](../mockup/group-type-org-after.html)。

| # | 問題 | 裁定（＝原建議）|
|---:|---|---|
| **1** | `group_type` 要哪幾個值？ | `department` / `process` / `announcement` / `test` 四個 · 夠用且語意清楚 |
| **2** | §2.3「報工及車輛調度是流程群」誰驗？ | M0.5 補跑數字 ＋ 問客戶（C2）· **不要憑名字判斷** |
| **3** | 回填自動猜還是人工判斷？ | **人工**。可以用「人數 ÷ 全體 > 60%」當提示，但不自動套 —— 猜錯會讓真部門從組織圖消失（F-2 P0）|
| **4** | 誰能改 `group_type`？ | v1 **aiproot only**；穩定後開放 tenant_admin（比照租戶自治的既有作法）|
| **5** | 流程群的訊息還要不要分析？ | **要**。報工群是任務主要來源，停掉等於自廢武功 |
| **6** | 只出現在全員群的那 10 人怎麼算？ | 歸「未分派」並在組織圖標示 —— 他們**確實**沒有部門訊號，不要硬塞 |
| **7** | 數字跳動怎麼跟客戶說？ | 切換當下出一份改前／改後對照 · 不可靜默替換（§5.1）|
| **8** | 部門健康度分母要不要同時改？ | **要**，且與 M2 同批上線 —— 組織圖與儀表講不同的話會更亂 |
| **9** | 「56 人 vs 12 帳號」納不納入本模組？ | **不納入**，另開一案。它是**導入問題**（要不要幫全員開帳號），本模組是**分類問題**，混在一起兩邊都做不完 |
| **10** | 一人真的身兼兩職（情況 A）怎麼辦？ | 本模組**不解**。等分類修完再量一次，若仍有顯著比例才談多對多 |

---

## 9. 失效場景反思（FMEA · R17 · M0 版）

| 路徑 | 失效模式 | 影響 | 嚴重度 | 緩解 |
|---|---|---|:--:|---|
| 材料化 | 誤以為「流程群不該有部門」而把 `department_id` 設 NULL | `tickets.department_id` NOT NULL → **建卡失敗，報工群的任務整批消失** | **P0** | ✅ §4.2 結構上避開：`department_id` 一律保留，只加 `group_type` |
| 回填 | 把真部門誤標成流程群 | 該部門**從組織圖消失**，成員變未分派 | **P0** | ⚠️ OQ-3 人工判斷 ＋ 可還原（改回 `department` 即可，無資料損失）|
| 統計 | 分母改了但沒告知 | 客戶看到健康度一夜變動，判定「系統壞了」 | **P1** | ⚠️ OQ-7 改前／改後對照 |
| 資料 | 測試群仍分派在部門（§2.4 已存在）| 該部門人數與健康度長期偏差 | **P1** | ⚠️ M0.5 修掉 · 這是**現在就在發生**的錯 |
| 上線 | 組織圖改了、健康度沒改 | 兩處對同一件事講不同的數字 | **P1** | ✅ OQ-8 同批上線 |
| 認知 | 客戶以為這解決了「員工跨部門」 | 若真有身兼兩職的人，期待落空 | P2 | ⚠️ 說明時講清楚：本次解的是**分類**，不是多對多（OQ-10）|

**M0 結論：兩個 P0 都圍繞同一件事 —— 分類改動不可以動到「任務掛在哪個部門」那條線。**
§4.2 把它結構性地隔開了，M1 實作時不得為了「乾淨」而把 `department_id` 清掉。

---

## 10. 變更紀錄

| 日期 | 版本 | 變更 |
|---|---|---|
| 2026-08-21 | v0.2 | 用戶「全採建議」裁定 OQ-GTC-1..10 · 加 UI mockup（`mockup/group-type-org-after.html`）待 review |
| 2026-08-21 | v0.1 | M0 DRAFT · 起於廠商展示指出「一位員工跨多部門」· prod 實測 66% 跨部門、「有你真好」40 人＝71%（全員群）· 根因＝三種群語意被塞進同一個模型 · 設計＝加 `group_type` 而非 `users` 多對多 · ⚠️ `tickets.department_id` NOT NULL 否決了「流程群不掛部門」的直覺解 · 待裁定 OQ-GTC-1..10 · 待客戶回答 C1–C4 |
