# task-to-personal-report · 任務自動歸屬當責人並匯入其個人日報

> 狀態：🚧 **M0 DRAFT v0.1**（2026-07-27）· 待用戶裁定 OQ-TPR-1..11
>
> 相關：[`warroom-task-board.md`](warroom-task-board.md)（任務來源）、[`personal-daily-report.md`](personal-daily-report.md)（匯入目標）、[`employee-line-binding.md`](employee-line-binding.md)（身分基石）
>
> ⚠️ 本模組會**把 A 的工作內容寫進 B 的日報**。對錯人不只是體驗問題，是資料錯誤＋隱私外洩。設計主軸是「寧可不歸屬，不可歸錯人」。

---

## 0. 觸發與現況查驗

用戶提出三條需求，2026-07-27 逐條對照程式碼查驗：

| # | 需求 | 現況 | 證據 |
|---|---|---|---|
| ① | 群組 → 工作日誌 → 任務 | ✅ **已實作** | `line_message` → group_batch → `analysis_result.records` → `TicketMaterializerService` → `tickets` |
| ② | 私訊 → 個人日報 → 本人確認 | ✅ **已實作** | 私訊（`chat_context='personal'`）→ 17:30 `pdr` 排程 → `personal_daily_report` → `POST /mine/save`（本人才能存） |
| ③ | 任務 → 個人日報（自動歸屬當責人並同步匯入） | ❌ **未實作** | 見下 |

### 0.1 ③ 為什麼算「未實作」

**「自動歸屬當責人」只做了一半**：`tickets.assignee_display_name` 有值，但它**只是 AI 從對話抽出的一段文字**。schema 註解自己寫得很清楚：

```sql
COMMENT ON COLUMN tickets.assignee_display_name IS
  '從對話抽的「派給誰」· 為顯示用文字 · 未來可 JOIN line_member.display_name 對到 user';
```

沒有 `assignee_user_id`。**系統知道對話裡出現了「阿明」，但不知道阿明是誰。**

**「同步匯入日報」完全沒有**：個人日報的資料來源只有一條，且只吃本人的私訊：

```sql
FROM line_message
WHERE sender_user_id = ${userId} AND chat_context = 'personal'
```

`personal-daily-report/` 全目錄 grep `ticket` → **0 筆**。兩個模組之間沒有任何連結。

---

## 1. 問題拆解 —— 這是三個獨立問題，難度差很多

| # | 問題 | 難度 | 風險 |
|---|---|---|---|
| **P1** | **身分解析**：人名文字 → `user_id` | **高** | **對錯人＝把 A 的工作寫進 B 的日報** |
| **P2** | 資料匯流：個人日報要吃第二種來源 | 中 | 覆蓋本人已編輯的內容 |
| **P3** | 時序：任務比日報晚出現 | 中 | 已確認的日報被偷改 |

**P1 是真正的難題**，P2/P3 是工程問題。把三者當成「接個線」會出事。

---

## 2. 核心原則：寧可不歸屬，不可歸錯人

把 A 的任務塞進 B 的日報，同時造成三件事：

1. **資料錯誤** —— B 的日報出現他沒做過的事
2. **隱私外洩** —— B 看到 A 的工作內容（客戶名、金額、異常）
3. **信任崩塌** —— 一旦發生一次，使用者會開始懷疑**所有**自動歸屬的內容

而「沒有自動歸屬」的代價只是：**維持現狀**，本人自己私訊回報。

**代價完全不對稱 → 判準是：不確定就不歸屬，並且要讓人看得到「有一筆待認領」。**

---

## 3. P1 · 身分解析

### 3.1 身分鏈是通的（已有基礎建設）

```
AI 抽出的 person 文字（「阿明」）
        ↓  ← 這一段是缺的
line_member.display_name   （該群成員的 LINE 顯示名）
        ↓  line_member.user_id (Uxxx)
user_line_binding.line_user_id  （status='active'）
        ↓
user_line_binding.user_id  →  users（系統帳號）
```

後三段都已存在（[`employee-line-binding`](employee-line-binding.md) 已 SHIPPED）。**只缺第一段。**

### 3.2 ⭐ 設計主張：在抽取階段 grounding，不要事後模糊比對

多數人的直覺是「先抽出人名，之後再做模糊比對」。**這是錯的方向**：

| 事後模糊比對 | 抽取階段 grounding |
|---|---|
| 候選集是「全公司的人」 | 候選集是**這個群當天有講話的成員**（通常 3–10 人） |
| 要處理「阿明／明哥／李明」的別名問題 | LLM 看得到實際名單，直接輸出名單中的值 |
| 比對邏輯自己寫，錯了難以調整 | 對不到就輸出 `null`，是模型能力範圍內的事 |
| 與 CLAUDE.md 既有做法不一致 | **與主檔 grounding（R11）一致** —— 本專案本來就這樣做 |

**做法**：分析該群當日對話時，把 `line_member` 的成員名單（限該群、限已綁定者）放進 prompt，要求 `person` 欄位**只能輸出名單中的顯示名，或 `null`**。

> 承 CLAUDE.md R11：「缺漏欄位填 null，禁止讓模型臆測」。人名尤其不可臆測。

### 3.3 歸屬信心度（三態，不是二元）

| 狀態 | 條件 | 行為 |
|---|---|---|
| **已歸屬** | person 精確命中名單中**唯一**成員，且該成員已綁定 | 寫入 `assignee_user_id`，可匯入其日報 |
| **待認領** | person 有值但對不到（未綁定／同名多人／不在名單） | **不歸屬**，任務看板顯「待認領」，可由主管指派或本人認領 |
| **無指派** | person 為 null | 維持現狀，不做任何事 |

**「待認領」是這個設計的關鍵**——沒有它，系統只能在「猜」和「丟掉」之間二選一，兩個都不對。

### 3.4 同名怎麼辦

同一群裡兩個「小明」→ **一律進「待認領」**，不用任何啟發式規則（誰講話多、誰先加入…）猜測。
猜對沒有獎勵，猜錯要付上面三種代價。

---

## 4. P2 · 匯流進個人日報

### 4.1 資料模型

個人日報的 item 目前是自由 jsonb（`ai_items` / `final_items`）。加一個來源標記即可，**不新增表**：

```jsonc
{ "title": "...", "detail": "...", "time": "...", "followup": "...",
  "source": "personal_dm" | "assigned_task",     // 新增
  "ticketId": "uuid"                             // 僅 assigned_task 有
}
```

### 4.2 三條紀律

1. **不覆蓋本人編輯過的內容**。`final_items` 一旦存在就是本人的版本，任務只能**追加**，不能改動既有項目。
2. **已確認（`status='confirmed'`）的日報不自動塞東西**。承 2026-07-27 裁定「AI 是輔助，不自動重跑」——新任務走 §5 的待整理區。
3. **來源要看得見**。匯入的項目在 UI 標「來自任務看板」並可點回原任務。使用者要能分辨「我自己講的」與「系統給我的」。

---

## 5. P3 · 時序

**問題**：日報 17:30 生成，群組分析 08:00 跑（或當日手動觸發），任務可能在日報之後才出現。

**建議**：沿用 2026-07-27 已上線的「**尚未整理進日報**」pattern —— 不重新生成、不偷改，而是在日報頁顯示：

```
有 2 項指派給你的任務尚未加入日報
  · 客戶 A 側踏改裝圖面待確認    [加入日報]
  · 3 號機軸承叫料              [加入日報]
```

**由本人決定要不要納入。** 這同時解掉「已確認日報被偷改」與「歸屬錯了本人可以不收」兩個問題——**本人是最後一道防線**。

---

## 6. 權限與隱私

| 誰 | 看得到什麼 |
|---|---|
| 本人 | 自己日報中的任務項目（含來源連結） |
| 主管 | **任務看板**上的任務（原本就看得到）；**看不到**別人的日報 |
| 未歸屬的任務 | 只在任務看板，不進任何人的日報 |

**不新增權限**：日報走本人 `user_id`，任務走既有 tickets RLS（部門範圍）。兩邊都已有正確的範圍控制。

> ⚠️ 一個必須擋的洩漏路徑：任務可能來自**本人不在的群組**。匯入時只帶 `summary`，
> **不帶原始對話**——否則等於讓人看到他沒參與的群組內容。要看來源請走任務看板（那裡有部門 RLS 把關）。

---

## 7. 失效場景反思（FMEA · R17）

| # | 路徑 | 失效模式 | 影響 | 嚴重度 | 緩解 |
|---|---|---|---|---|---|
| F-1 | 身分解析 | 抽到的人名對到錯的人 | **A 的工作進 B 的日報 + B 看到 A 的內容** | **P0** | §3.2 抽取階段 grounding 限定候選集；§3.3 對不到一律「待認領」；§5 本人是最後一道防線 |
| F-2 | 身分解析 | 同群同名兩人 | 同上 | **P0** | §3.4 一律待認領，不用啟發式猜測 |
| F-3 | 隱私 | 匯入時連原始對話一起帶 | 看到自己沒參與的群組內容 | **P0** | §6 只帶 summary，來源走任務看板 |
| F-4 | 匯流 | 蓋掉本人已編輯的 `final_items` | 使用者的修改消失 | **P0** | §4.2 只追加不改動；已確認的走待整理區 |
| F-5 | 時序 | 已確認日報被自動改寫 | 使用者發現「我確認過的東西自己變了」 | P1 | §5 不自動寫入，由本人加入 |
| F-6 | 重複 | 任務被 rerun 重新材料化 → 重複匯入 | 日報出現兩筆一樣的 | P1 | 以 `ticketId` 去重；`ux_tickets_source_record` 已保證 ticket 不重複 |
| F-7 | 迴圈 | 日報內容再被分析成任務 → 無限循環 | 資料爆炸 | ~~P1~~ | ✅ **已驗證不會發生**：PDR 走獨立 pipeline，`uploadId: null`、不進 `analysis_upload`，materializer 掃不到 |
| F-8 | 綁定 | 員工未綁定 LINE → 永遠對不到 | 該員工的任務都在待認領 | P1 | 待認領清單顯示「原因：未綁定 LINE」，導向綁定流程 |
| F-9 | 離職 | 綁定被 revoke 後仍歸屬舊任務 | 已離職者的日報仍在產生 | P2 | 歸屬時檢查 `status='active'` |
| F-10 | 體感 | 待認領一直沒人處理，越積越多 | 功能形同虛設 | P1 | 任務看板顯示待認領數；超過門檻提醒主管 |

**P0 共 4 條**，全部圍繞同一件事：**不要把工作歸給錯的人**。

---

## 8. 開放問題（OQ-TPR-N）

| # | 問題 | 建議 |
|---|---|---|
| **OQ-TPR-1** | 身分解析走「抽取階段 grounding」還是「事後模糊比對」？ | **抽取階段 grounding**（§3.2）· 與 R11 主檔 grounding 一致 |
| **OQ-TPR-2** | 對不到人時：待認領 / 丟掉 / 猜一個？ | **待認領**（§3.3）· 三態不是二元 |
| **OQ-TPR-3** | 同名多人怎麼辦？ | **一律待認領**，不用啟發式（§3.4） |
| **OQ-TPR-4** | 匯入是自動寫進日報，還是由本人「加入」？ | **本人加入**（§5）· 本人是最後一道防線 |
| **OQ-TPR-5** | 已確認的日報要不要能再加任務？ | 要，但走待整理區、不改既有內容 |
| **OQ-TPR-6** | 匯入的項目本人可以刪除嗎？ | **可以** · 歸屬錯了要有出口；刪除記 audit 供改進抽取 |
| **OQ-TPR-7** | 未綁定 LINE 的員工怎麼處理？ | 待認領 + 顯示原因 + 導向綁定 |
| **OQ-TPR-8** | 待認領由誰指派？主管 / 本人認領 / 都可？ | **都可**（主管在看板指派、本人在日報認領） |
| **OQ-TPR-9** | 要不要通知被指派的人？ | v1 **不主動通知**（避免雜訊）· 待累積使用經驗再決定；要做的話走 notification-hub |
| **OQ-TPR-10** | 需要 `assignee_user_id` 欄位嗎（migration）？ | **需要** · 目前只有文字，無法做任何可靠關聯 |
| **OQ-TPR-11** | 這件事屬 ai-center-line 還是 EEA/SAM？ | 傾向本專案（緊貼既有 tickets / PDR），但與 [`business-query-assistant`](business-query-assistant.md) OQ-BQA-2 是同一類問題，建議一起裁定 |

---

## 9. 里程碑

| 里程碑 | 內容 |
|---|---|
| **M0** | 本文件 + OQ 裁定 ← 目前在這 |
| **M1** | migration：`tickets.assignee_user_id` + 歸屬狀態；抽取 prompt 加群成員 grounding |
| **M2** | 歸屬解析服務（唯一命中才寫入 / 其餘待認領）+ 單元測試（同名、未綁定、離職） |
| **M3** | 任務看板顯示歸屬狀態 + 待認領指派 |
| **M4** | 個人日報「尚未加入的任務」區塊 + 加入／刪除 |
| **M5** | FMEA 覆核（4 個 P0）+ prod migration + smoke |

---

## 10. 變更紀錄

| 日期 | 版本 | 變更 | 作者 |
|---|---|---|---|
| 2026-07-27 | v0.1 | M0 首版 · 三條需求逐條查驗（①②已實作、③未實作）· 拆成身分解析／匯流／時序三個問題 · 主張抽取階段 grounding 取代事後模糊比對 · 歸屬三態（已歸屬／待認領／無指派）· 本人為最後防線 · FMEA 10 條含 4 個 P0 · OQ-TPR-1..11 | ahern + Claude Code |
