# 設計文件 · 台灣福祉 2026-08-24 修改建議

> ✅ **SHIPPED v1.0**（2026-08-25）· 10 項裡 **8 項已交付**，剩 2 項卡在等客戶回話
> 來源：`2026.08.24台灣福祉戰情室修改建議.docx`（10 項）
> 相關：[`media-and-vision.md`](media-and-vision.md)、[`task-completion-tracking.md`](task-completion-tracking.md)、
> [`custom-roles.md`](custom-roles.md)、[`notify-tenant-scoping.md`](notify-tenant-scoping.md)
>
> **一句話**：10 項裡 **3 項本來就已經能用只是客戶不知道**、**2 項是命名造成的誤解不是 bug**、
> 真正要寫程式的只有 5 項 —— 其中 4 項已完成，剩⑧會議記錄要先問客戶才動得了。
>
> | 狀態 | 項目 |
> |---|---|
> | ✅ 已交付（本批）| ①素材下載 ②素材篩選 ③改名核對 ④知會其他人 ⑥存查分頁篩選 ⑦隱藏成本欄位 ⑧a狀態誤判 |
> | 🟢 早就能用（要教）| ⑨人資角色 ⑩改密碼 |
> | ⏸ 等客戶回話 | ⑤（OQ-4：畫面還是通知）· ⑧顆粒度（OQ-9：會議記錄還是決議待辦）**—— 只剩這兩題** |
>
> ⚠️ **FMEA 見 §8** —— 三個 P0 皆已緩解。上 prod 前必讀 §8.1 部署路徑。

---

## 1. 逐項判定（先分類，再談做法）

⚠️ 每一項都對照過實際程式碼，不是憑印象。判定依據列在各項下面。

| # | 需求（客戶原話）| 交付 | 還缺 |
|---|---|---|---|
| ① | 素材檔案可以下載 | ✅ 下載按鈕（`7d32d02`）| — |
| ② | 素材加搜尋（**日期／關鍵字**）| ✅ 日期＋群組**篩選**（`be42ab7` `0051af5`）| ⚠️ **關鍵字沒做**（OQ-2 裁定先做兩個篩選）|
| ③ | 「已簽核」又寫「卡住 24 天」矛盾 | ✅ 全站改名 簽核→核對（`bbe74d8` `bef74e6`）| — |
| ④ | 指派時同時通知其他人 | ✅ 知會其他人 · 只發私訊（`d8482da` `4bca96b`）| — |
| ⑤ | 任務先給部門主管、需要才給總經理 | — | ⏸ **卡 OQ-TWH-4**（畫面還是通知）|
| ⑥ | 已簽核的資料存哪 ＋ **搜尋** | ✅ 存查獨立分頁＋日期／群組篩選（`8f419c9` `f086993`）| ⚠️ **關鍵字沒做**（同 ②）|
| ⑦ | 「併發限制」是什麼功能 | ✅ 租戶端整塊不渲染（`7d32d02`）| — |
| ⑧ | 會議記錄讓 AI 判讀、進「會議記錄區」 | 🟡 **只修了狀態誤判**（`3aba0e2`）| ⏸ **卡 OQ-TWH-9** —— 真正的缺口是抽取顆粒度（§5.3.2）|
| ⑨ | 後台人員管理開權限給人資 | 🟢 **早就能用**（自訂角色 08-21）| 📮 **客戶還不知道** —— 待寄 §9 回覆稿 |
| ⑩ | 總經理要改密碼 | 🟢 **早就能用** | 📮 **客戶還不知道** —— 同上 |

> ✅ 已交付 ｜ 🟡 部分 ｜ 🟢 本來就有 ｜ ⏸ 卡等客戶 ｜ 📮 卡在我們還沒說
>
> ⚠️ **「篩選」不等於客戶寫的「搜尋」** —— ②⑥ 兩處客戶都寫了「搜尋」，
> 我們交付的是日期／群組**篩選**。這是 OQ-TWH-2「先這兩個」的裁定結果，不是漏做，
> 但**客戶要的還沒給全**：他若心裡想的是「打幾個字找那張報價單」，用了還是會回頭反應。
> `tickets.summary` / `line_media.original_filename` 的 ILIKE 都是很小的追加。

---

## 2. 🟢 已經可以用，但客戶不知道（⑨⑩）

### 2.1 ⑩ 總經理改密碼 —— 一直都能改

`POST /auth/change-password` 存在，`ChangePasswordDialog` 也掛在 `Shell.tsx`，
入口在**右上角頭像 → 選單 → 變更密碼**，任何登入的人都能用。

**這不是功能缺口，是找不到。** 而且它正好落在同一個病灶上：
使用者選單藏在頭像後面，沒點開不知道裡面有什麼。

> ⭐ 這一項不要寫成「開發項目」回覆客戶 —— 那會讓對方以為我們沒做過。
> 正確回法：「已經可以了，位置在這裡（附截圖）」。

### 2.2 ⑨ 人資要能管人員 —— 自訂角色 2026-08-21 已上線

「後台人員管理開權限給人資」＝ 建一個只能管成員、看不到其他東西的角色。
`custom-roles` v0.3（`dbb5996` / `9cd7263`）已經做完並上 prod：

```
設定 → 權限管理 → ＋ 建立角色
  名稱：人資
  看得到誰的資料：只看自己 / 只看自己部門 / 看全公司  ← 三選一
  可以做哪些事：勾「查看成員」「分配部門」…
再到 部門/成員 把那個人的角色改成「人資」
```

⚠️ **這正好是 OQ-CR-7 一直缺的那個答案**（「客戶到底要哪個自訂角色」）——
現在有了：**人資**。任務 #34 可以用這個跑驗收。

⭐ **範圍不用問客戶**（2026-08-25 用戶裁定）—— 「看得到誰的資料」本來就是建立角色時的三選一欄位，
客戶自己選就好。先問他等於要他在還沒看到畫面前先想好答案，**多一次判斷**。

我們只要告訴他入口在哪，以及一件他自己看不出來的事：
> 選「只看自己部門」的話，**那位同仁必須先被指定部門**，否則他會什麼都看不到（V-7）。

---

## 3. 🟠 誤解與文案（③⑤⑦）

### 3.1 ⭐ ③「已簽核」又「卡住 24 天」—— 兩個不同的軸，不是矛盾

看程式碼（`warroom-tasks.service.ts:213`）：

```ts
const isStuck = (r) =>
  r.work_status === "open"          // 事情還沒做完
  && r.confirm_status === "已簽核"   // 但主管已經確認過「AI 讀對了」
  && daysSince(r.created_at) > GRACE_DAYS;
```

| 欄位 | 真正的意思 | 總經理以為的意思 |
|---|---|---|
| `confirm_status = 已簽核` | **確認 AI 整理得對**（這件事真的存在、內容沒抽錯）| ❌「這件事處理完了」|
| `work_status = open` | 事情**還沒做完** | — |

**所以「已簽核 + 卡住 24 天」＝「你確認過這件事是真的，但 24 天沒人去做」——
邏輯完全一致，是「簽核」這個詞被誤讀。**

⭐ 這跟 `recordStatusLabel.ts` 早就記過的問題同源：
> `resolved` 刻意**不叫「已完成」**：那是 AI 讀到的（推論），而「完成」是本人回報的（承諾）。
> 兩者用同一個詞的話，看板上會出現「已完成但尚未確認完成」這種自相矛盾的句子。

**當時把這條紀律套在 `records` 上，卻沒套在 `tickets` 的「簽核」上。**

**建議改法（純文案，不動資料）**：

| 現在 | 改成 |
|---|---|
| 已簽核 | **已確認**（＋ tooltip：「主管確認過 AI 整理得對；事情做完沒有另外看『進度』」）|
| 卡住 24 天 | **確認後 24 天沒進度** |
| 待簽核 | **待確認** |

### 3.2 ⑤ 任務先給部門主管、需要才給總經理

**資料層本來就是這樣**：`tickets` 的 RLS 讓 `group_owner` 只看得到自己部門的任務，
`tenant_admin` 才看全公司。所以「總經理看到全部」是**他的角色本來的設計**，不是漏擋。

⚠️ 但客戶說的可能是**通知**不是**畫面** —— 現在還不確定他指哪一個：
- **(a) 畫面**：已經是分層的，不用改（要說明給他聽）
- **(b) LINE 通知**：目前指派通知只發給**被指派的那個人**（`assign-notify.service.ts`），
  沒有「先通知主管、主管決定要不要往上報」這條鏈
- **(c) 總經理不想看到全部**：那是他自己的角色範圍太寬 —— 可以用自訂角色做一個
  「只看摘要不看細節」的視角，但這是產品方向不是 bug

**這一項不寫需求，先問清楚**（OQ-TWH-4）。

### 3.3 ⑦「併發限制」是什麼 —— 客戶不該看到這個欄位

`concurrency` 是**分析批次同時跑幾個群組**的技術參數，屬成本控管。
`scheduler-config.service.ts:11` 的註解自己就寫了：

> 欄位級 whitelist · **tenant_admin 不能改**成本控管欄位（concurrency / lookback_days）

**改不了卻看得到 —— 那就是純粹的困惑來源。** 客戶會問「這是什麼」正是這個原因。

**建議**：租戶端**不顯示** `concurrency` 與 `lookback_days`（aiproot 端保留）。
這跟 08-21 那個「助理角色看得到、改不動、也用不了」是同一類。

---

## 4. 🟡 差最後一哩（①⑥）

### 4.1 ① 素材下載 —— 後端已經有了

`GET /media/:mediaId/content`（`media:view`）已存在，客戶的記憶是對的。
它現在回 `content-disposition: inline`（給縮圖與預覽用）。

**要做的**：
- 前端加「下載」按鈕
- ~~下載時要 `attachment` 而不是 `inline` → 加一個 `?download=1` 參數~~
  → **實作時發現不需要**：前端本來就是帶 JWT 取 blob 再存檔（`downloadMedia`），
  存檔名走清單本來就有的 `filename`。加了那個參數會是沒人呼叫的死碼，已拿掉。
- 中文檔名已經走 RFC 5987，不用另外處理

⚠️ **不要**用前端 `<a download>` 硬幹：跨來源時瀏覽器會忽略 `download` 屬性，
變成直接開新分頁。要在後端下對 header。

### 4.2 ⑥ 已簽核的資料存哪 + 搜尋

**存在「存查」頁**（任務看板右上「存查」按鈕）—— 客戶可能沒發現。
但目前有兩個實際限制：

```ts
const archived = notTracked.slice(0, 50);   // warroom-tasks.service.ts:296
```

- **只回 50 筆**，沒有分頁 → 累積久了看不到舊的
- **沒有搜尋**

**✅ 已做（M3b · 2026-08-25）**：改成獨立分頁查詢 ＋ 日期／群組篩選。

⭐ 實作時才發現天花板是**雙層**的，而外層那層沒有人看得見：

```
/warroom/tasks 撈最近 500 筆 → JS 分堆 → 存查 slice(0, 50)
```

超過 500 張票的租戶，較舊的存查紀錄**根本沒被撈進來**，畫面上那個「共 N 筆」也是錯的。
而存查正好是唯一一個**必須看得到舊資料**的頁面。

⭐⭐ 更糟的是那個錯的數字還管著入口：前端用 `counts.archived > 0` 決定要不要畫
「存查」按鈕 —— 數字掉到 0，**整個入口就消失了**，使用者再也點不進那一頁。
所以 `counts.archived` 也改成獨立 `count(*)`。

⚠️ **關鍵字搜尋沒做** —— 依 OQ-TWH-2 的裁定只做日期＋群組。
若客戶用了之後仍找不到，`tickets.summary` 的 ILIKE 是很小的追加。

---

## 5. 🔵 要開發（②④⑧）

### 5.1 ② 素材搜尋 —— ✅ 已做（日期＋群組篩選）

原本只有「類型」篩選。**用戶 2026-08-25 裁定「先日期、群組這兩個」** ——
是**篩選**不是全文檢索，所以不碰訊息內容比對，關鍵字那條先不做。

已實作（`be42ab7` 後端 / `0051af5` 前端），三個 happy path 測不到的點釘在
`server/test/media-filters.test.ts`：

| 陷阱 | 沒處理會怎樣 |
|---|---|
| 日期要用**台灣時間**的那一天 | `sent_at` 存 UTC，台灣早上 8 點前傳的檔案算成前一天 —— 使用者選「今天」看不到今天早上的圖，而且不會覺得是篩選壞了，只覺得**檔案不見了** |
| `counts` 要吃同一組篩選 | 分頁籤寫「圖片 135」，點下去只有 3 張 |
| 群組下拉排除 1:1 私訊 | 私訊的 `group_id` 是 `__personal__<userId>` 佔位，列進下拉是一串亂碼 |

⚠️ 群組下拉刻意**不吃**日期／群組篩選 —— 選項會隨條件消失的下拉，選下去就切不回來。

⚠️ 順帶發現（未處理）：**1:1 私訊傳的檔案會出現在素材看板**。
`department_id` 為 null 所以部門主管看不到，但 tenant_admin 看得到。
這是既有行為不是這次改的，但值得跟客戶確認是否符合預期。

### 5.2 ④ 指派時同時通知其他人

現況 `assign-notify.service.ts` 只通知**被指派的那一位**。

要決定的是**通知誰**：
- (a) 固定加上該部門主管
- (b) 指派時可以自己勾要一起通知的人
- (c) 通知到 LINE 群組並 @ 那些人 ← 這條**已經有 M0**：[`notify-group-mention.md`](notify-group-mention.md)

⚠️ **注意 memory `project_line_group_never_speaks`**：2026-08-12 已裁定
**LINE 群組一律不出聲**（reply 是全群可見）。所以 (c) 要走**主動 push 到群**，
不是 reply，而且要重新確認那條裁定涵不涵蓋這種情境（OQ-TWH-5）。

### 5.3 ⑧ ⭐ 會議記錄 —— 最大的一項，且**不能直接開工**

客戶的原話：
> 他們有一整坨的會議記錄，可能有回覆所以直接進到已簽核，但這個項目還沒完成。
> 不知道會議記錄這個部分能否讓 AI 判讀並且進到會議記錄區之類的

拆開來其實是**三個不同的問題**：

| | 問題 | 性質 |
|---|---|---|
| a | 「有回覆就直接進已簽核」 | ⚠️ **可能是 bug** —— 要查完成訊號的判定是不是太寬鬆 |
| b | 「這個項目還沒完成」 | 同 ③ —— 已簽核 ≠ 已完成，可能又是命名誤解 |
| c | 「進到會議記錄區」 | 🔵 新功能：會議記錄要獨立成一個區域／分類 |

⚠️ **c 有一個現成的機制被忽略了**：AI 已經可以自己造分類，而 08-24 剛修好
「新分類必須用繁體中文」（`a477a61`）。所以「會議記錄」**可能不需要寫程式** ——
它可以是一個分類，在「任務設定 → 分類詞庫」裡管理。

但要先確認 a：**如果會議記錄是因為 bug 才跑進已簽核，那先修 bug，不是加新區域。**

---

#### 5.3.1 a 的查核結果（2026-08-25 · 任務 #37）

**結論：不是 bug，但客戶的直覺指到了一個真的缺口。**

| 客戶說的 | 實際上 |
|---|---|
| 「直接進到**已簽核**」 | `laneFor()` **從來不會回已簽核** —— 材料化只產生 待簽核／待確認／存查。已簽核只有人按下去才有 |
| 客戶看到的其實是 | **存查**（`high` + `resolved` → 存查），而存查頁副標寫著「公告 / 已完成 / 已忽略 · **不需核對的紀錄**」，讀起來就是做完了 |
| 「有回覆」導致的 | ✅ 這一段是真的 —— 但問題在 **prompt**，不在判定邏輯 |

**根因**：`tenant-twh.ts` 抽取規則 5 原本只有一行、沒有任何限定：

```
5. status：open = 尚未處理、in_progress = 處理中、resolved = 已解決、info = 純資訊/知識。
```

會議記錄裡有人回「收到」，模型判成 `resolved` 完全講得通 ——
**「訊息被回應了」被讀成「事情做完了」**。這跟 08-24 分類中英混雜是同一類：
prompt 太薄，模型用一個講得通但不對的讀法填空。

**已修（M0.6）**：規則 5 補上限定 ——「有人回覆不等於事情做完」、列出實際會出現的
回覆詞、明講「會議開完了」不算 resolved、以及**判斷不了就填 open**（兩邊錯誤成本
不對稱：誤填 open 是多一次 30 秒核對，誤填 resolved 是那件事沒人再看到）。
`server/test/status-resolved-semantics.test.ts` 釘住這幾句不被後續改 prompt 弄丟。

**順帶查到的好消息**：`resolved` **不會**讓卡片離開工作生命週期 ——
`workStatusFor()` 只擋紀錄類分類與 `info`（de8d291 保守版的裁定）。
所以誤判 resolved 的票在當責人的待辦清單上還在，只是簽核分區顯示成「存查」。
**這次只需要修 prompt，不用動 work_status。**

#### 5.3.2 ⭐ 但真正的缺口比 a 深 —— 顆粒度，不是狀態

客戶說「**這個項目還沒完成**」。他要的不是那筆會議記錄的狀態，
是**會議裡決定要做的事**。

現在 prompt 的規則 1 是「一事一筆」，而 AI 把「開了一場會」當成那個「事」：

| | 現況 | 客戶要的 |
|---|---|---|
| 一場會議 | 抽成 **1 筆**「會議記錄」→ 存查 | 抽出**每一項決議** → 各自成為一張待辦卡 |

**這不是狀態判錯，是抽取顆粒度不對。**
加一個「會議記錄區」治不了它 —— 那只是把同一筆東西換個地方放。

⚠️ **動之前必須確認的**：`templates.ts` 規則 14 已經在處理「線上週會議／部門週會」
這類項目 —— 但那是**報工單（service_reports）**的 items 規則，跟這裡的 `records`
是不同 schema。改 records 的顆粒度不會直接打到日報抽取，但**要一起回歸驗證**（R12）。

⚠️ 另一個風險：一場會抽 5–10 筆決議，**待核對佇列的量會跳一個級距**。
主管每天 3 分鐘的核對節奏可能被打破 —— 這是要跟客戶講清楚的取捨，不是實作細節。

---

## 6. 開放問題（OQ-TWH-N）

| # | 問題 | 建議 |
|---|---|---|
| **OQ-TWH-1** | ③ 的「簽核」要不要改名成「確認」？ | ✅ **裁定：全站改名** · 含手冊、LINE 通知文案、簽核率指標 · **一次改完**（改一半就是 0069 那個教訓）|
| **OQ-TWH-2** | ② 素材搜尋的關鍵字比對什麼？檔名／群組名／該則訊息內容？ | ✅ **裁定（2026-08-25 用戶）：先做日期＋群組兩個篩選** · 是**篩選**不是全文檢索，不碰內容比對 · 已實作 `be42ab7` `0051af5` |
| **OQ-TWH-3** | ⑦ 租戶端要不要直接隱藏 concurrency / lookback_days？ | ✅ **裁定：隱藏** · aiproot 端保留 |
| **OQ-TWH-4** | ⑤ 客戶指的是**畫面**還是**通知**？ | **需問客戶** · 畫面已經分層了，通知沒有 |
| **OQ-TWH-5** | ④ 要不要通知到 LINE 群？ | ✅ **裁定：只發個人私訊** · 指派時可勾額外要通知的人，各自收到 LINE 私訊 · **不碰群組** —— 2026-08-12「LINE 群組一律不出聲」的裁定維持不變 |
| **OQ-TWH-6** | ⑧a 會議記錄「因為有回覆就進已簽核」是不是 bug？ | ✅ **已查（#37 · §5.3.1）：不是 bug** · 沒有自動進已簽核的路徑；客戶看到的是「存查」· 根因是 prompt 對 `resolved` 沒有限定 → **M0.6 已修** |
| **OQ-TWH-7** | ⑧c「會議記錄區」是新分類就夠，還是要獨立頁面？ | ⏸ **兩者都不對** · 查下來真正的缺口是**抽取顆粒度**（§5.3.2）—— 會議該抽出「每一項決議」而不是「開了一場會」。加區域／加分類都只是換位置擺。→ 轉 **OQ-TWH-9** |
| **OQ-TWH-9** 🆕 | 客戶要的是**會議記錄本身**（存起來可查），還是**會議裡的待辦**（各自追蹤到完成）？ | **需問客戶** —— 決定要不要改抽取顆粒度。若是後者，待核對佇列的量會跳一個級距，要先講清楚取捨 |
| **OQ-TWH-8** | ⑨ 人資角色要看**全公司**還是**只有自己部門**的人？ | ✅ **裁定（2026-08-25 用戶）：不問** —— 那本來就是客戶在「建立角色」時自己三選一的欄位。問他等於要他先想好答案再操作，多一次判斷 · 我們只要告訴他入口在哪 |

---

## 7. 里程碑

| # | 內容 | 依賴 | 估 |
|---|---|---|---|
| **M0** 📋 | 本文 · 待裁定 OQ-TWH-1..8 | — | ✅ |
| **M0.5** | ⭐ **先回客戶「這 3 項已經可以用了」**（⑨⑩＋⑥的存查頁）· 附操作位置截圖 | — | 立刻 |
| **M0.6** | ✅ 查 ⑧a ＋ 修 prompt 對 `resolved` 的限定（§5.3.1）| OQ-6 | ✅ |
| **M1** | ✅ ⑦ 租戶端隱藏成本控管欄位 ＋ ① 素材下載按鈕（`7d32d02`）| OQ-3 | ✅ |
| **M2** | ✅ ③ 簽核→核對 全站改名（含手冊）（`bbe74d8` `bef74e6`）| OQ-1 | ✅ |
| **M3a** | ✅ ② 素材依日期＋群組篩選（`be42ab7` `0051af5`）| OQ-2 | ✅ |
| **M3b** | ✅ ⑥ 存查獨立分頁查詢＋日期／群組篩選（`951b843` `8f419c9` `f086993`）| — | ✅ |
| **M4** | ✅ ④ 指派通知擴充 · 只發個人私訊（`d8482da` `4bca96b`）| OQ-5 | ✅ |
| **M5** | ⏸ ⑧ 會議決議抽取顆粒度（**不是**加「會議記錄區」）· 需回歸驗證 R12 | **OQ-9**（未問）| L |

⚠️ **M0.5 排第一** —— 有 3 項客戶以為缺的東西其實早就有了。
先回這個，客戶對進度的感受會完全不同，而且成本是零。

### 7.1 目前卡在哪（2026-08-25）

程式碼這邊做得完的**都做完了**。剩下 3 項全部卡在同一個地方：**問客戶**。

| 卡住的 | 要問什麼 | 為什麼不能猜 |
|---|---|---|
| ⑤ | **OQ-TWH-4**：指的是**畫面**還是**通知**？ | 畫面已經分層了；通知沒有。搞錯方向等於做了不需要的東西 |
| M5（⑧）| **OQ-TWH-9**：要**會議記錄**還是**會議裡的待辦**？ | 決定要不要改抽取顆粒度 —— 影響待核對佇列的量級 |

剩下的都要客戶回話。程式碼這邊做得完的**都做完了**（M1／M2／M3a／M3b／M4／M0.6）。

＋ **OQ-TWH-8**（⑨人資角色看全公司還是只有自己部門）不擋開發，但擋驗收。

⚠️ M1／M2／M4 已 commit 但**尚未 push** —— 客戶那邊還看不到。

---

## 8. 失效場景反思（FMEA · R17）

> 逐路徑跑一次 pre-mortem：假設它已經壞了，反推為什麼。
> 涵蓋本批已實作的 M0.6 / M1 / M2 / M3a / M3b / M4。
> **任一 P0 未緩解不得上 prod。**

### 8.1 部署路徑（最容易被忽略的一條）

| # | 失效模式 | 影響 | 嚴重度 | 緩解 |
|---|---|---|---|---|
| **F-1** | **舊前端 + 新後端**：`/warroom/tasks` 拿掉 `kanban.archived`，使用者瀏覽器裡快取的舊 bundle 執行 `kanban.archived.map(...)` → `undefined.map` | 任務看板**整頁白畫面**。Render 是 rolling deploy，且前端 bundle 在使用者分頁裡可能已開數小時 | **P0** | ✅ 後端**保留 `archived: []`**（不是拿掉 key）· 舊前端最差只是存查看起來空的（degraded，不是壞掉）· `archived-tasks.test.ts` 釘住這個相容契約 |
| F-2 | 前後端分批上線：新前端打 `/warroom/tasks/archived`，後端還沒上 → 404 | 存查頁載入失敗 | P1 | ⚠️ **殘留**：靠「同一批 push、後端先起來」· Render 兩個 service 各自部署，仍有數十秒空窗 · 影響範圍是一個偶爾才開的次頁，可忍 |
| F-3 | 只中一次 smoke 就宣告部署完成，實際打在新舊混合的實例上 | 驗到的是舊版，錯的東西上線沒人知道 | P1 | 🔒 **外部 gate**：smoke 要**連續 3 次**打中新版才算數（`pitfall_render_rolling_deploy_verify`，同一天用錯 7 次） |

### 8.2 M0.6 · prompt 的 `resolved` 限定

| # | 失效模式 | 影響 | 嚴重度 | 緩解 |
|---|---|---|---|---|
| **F-4** | ⭐ `tenant-twh.ts` 是**所有走群組分析的租戶共用**的（`resolveTenant`：`slug === "twh" \|\| "batch"` → `TWH_TENANT`，而所有群組分析都帶 `batch`）—— 這次改動**其他客戶也吃得到**，他們沒要求過 | 其他租戶的抽取行為在他們不知情的狀況下改變 | P1 | ✅ 加的規則是**通用**的（「有人回覆不等於事情做完」對任何業種都成立），不是台灣福祉專屬 · ⚠️ 但這條路徑本身是 [`tenant-prompt-decoupling`](tenant-prompt-decoupling.md) F-1 記過的 P0，**接第二家客戶前必須解掉** |
| F-5 | 只對**新抽取**生效 · prod 現有的存查會議記錄不會自己搬家 | 客戶回報的那批舊資料看起來沒改善 | P2 | ⚠️ **殘留**：要改的話得**重跑抽取**（不是重跑材料化 —— lane 是從已存的 `status` 算的）· 回覆客戶時要講清楚「往後的會生效」 |
| F-6 | 反向誤判：真的做完的事被填 `open`，留在待核對 | 待核對佇列變長 | P2 | ✅ **刻意選的方向**：誤填 open 是多一次 30 秒核對，誤填 resolved 是那件事沒人再看到 —— 錯誤成本不對稱 |
| F-7 | system prompt 前綴改變 → prompt cache 失效重建一次 | 一次性成本 | P2 | ✅ 加 6 行不會掉到 Opus 的 4096 token 快取門檻以下（掉下去會**靜默不快取**，見 AGENTS.md） |

### 8.3 M3a / M3b · 篩選與分頁（兩頁同一組形狀）

| # | 失效模式 | 影響 | 嚴重度 | 緩解 |
|---|---|---|---|---|
| **F-8** | ⭐ 錯的數字管著入口：`counts.archived` 從「最近 500 筆」數，票一多就歸零，而前端用 `counts.archived > 0` 決定畫不畫「存查」按鈕 | **整個入口消失**，使用者再也點不進那一頁 | **P0** | ✅ 改成獨立 `count(*)`，不從看板那 500 筆數 · 測試釘住真實總數 |
| F-9 | 日期用 UTC 比 → 台灣早上 8 點前的資料算成前一天 | 使用者選「今天」看不到今早的東西，而且**不會覺得是篩選壞了，只覺得資料不見了** | P1 | ✅ 兩頁都用 `AT TIME ZONE 'Asia/Taipei'` · 兩支測試各自造邊界資料，拿掉時區轉換會紅 |
| F-10 | `counts` / `total` 沒吃同一組篩選 | 分頁籤寫「圖片 135」點下去只有 3 張；存查頁碼算出翻不到的頁 | P1 | ✅ 兩邊都吃同一個 `FILTER` · 已測 |
| F-11 | 亂填日期（`2026-02-30`）放行到 SQL → pg 22008 | 使用者拿到 500，看起來像系統壞了 | P1 | ✅ `common/query-date.ts` 往返比對擋在 controller（`Date.parse` 驗不出 2/30，V8 會進位成 3/2）· 已測 |
| **F-12** | 存查查詢**沒有任何 `WHERE tenant_id`**，整個靠 `tickets` 的 RLS | 少設一個 session 變數就靜默多回列＝**跨租戶外洩** | **P0** | ✅ 加了跨租戶＋跨部門隔離測試（`rule_rls_silent_zero` 已踩 10 次）· `total` 走另一條 count 查詢，也一併驗過範圍一致 |
| F-13 | 群組下拉隨日期篩選縮水 | 選了某一群之後選項只剩它自己，再也切不回去 | P2 | ✅ 下拉刻意**不吃**日期／群組篩選 · 已測 |
| F-14 | 群組很多的租戶，下拉會很長 | 難用 | P2 | ⚠️ **殘留**：目前最多 22 群，可忍 · 超過 ~40 群要加搜尋 |
| F-15 | `groups` 與 `count(*)` 是額外的查詢，資料量大時變慢 | 看板／素材頁載入變慢 | P2 | ⚠️ **殘留**：prod 目前 135 個檔案、數百張票，無虞 · **上萬筆時要看 `confirm_status` / `sent_at` 的索引** |
| F-16 | 1:1 私訊傳的檔案出現在素材看板（總經理室看得到） | 員工私下傳的東西被看到 | P1 | ⚠️ **殘留 · 既有行為不是這次改的**（任務 #40）· 群組下拉已排除，清單仍列 · **要跟客戶確認是否符合預期**，要擋是一行 |

### 8.4 M2 · 簽核→核對 改名

| # | 失效模式 | 影響 | 嚴重度 | 緩解 |
|---|---|---|---|---|
| F-17 | 改名只改一半（側欄改了頁內沒改、指路文字還寫舊名） | 客戶照著指示找不到東西 —— **比不一致更糟，是主動導錯方向** | P1 | ✅ `ui-naming-consistency.test.ts` 守住頁內標題／麵包屑／指路文字（`pitfall_rename_only_half_done`，一天犯三次的教訓） |
| F-18 | DB 值仍是「已簽核」，只有顯示層換 · 有人改了 `CONFIRM_STATUS_LABEL` 的 **key** | 比對全部失效，任務永遠卡在某個狀態 | P1 | ✅ 檔頭寫明「map 的 key 是 DB 值」· 比對一律用 DB 值 |
| F-19 | 客戶已經習慣「簽核」的講法，改名當下會困惑 | 支援成本 | P2 | 🔒 **外部 gate**：回覆客戶時要主動說明改名與理由（三條軸各用不同的詞） |

### 8.5 結論

**兩個 P0（F-1 部署白畫面、F-8 入口消失、F-12 跨租戶外洩）皆已緩解**，可上 prod。

殘留清單（皆 P1 以下，各有追蹤）：F-2 部署空窗、F-5 舊資料不回填、F-14/F-15 規模上限、
**F-16 私訊素材可見性（任務 #40 · 要問客戶）**。

---

## 9. 回覆客戶用（M0.5）

> 直接複製下面這段。**三個原則**：
> · ⑨⑩⑥ 不可以寫成「開發項目」—— 那會讓對方以為我們沒做過。要寫「已經可以了，位置在這裡」
> · 改名（簽核→核對）是他看得到的變化，**要主動說**，不然他會以為系統換了
> · 問題放最後，而且要問得夠具體，讓他一句話就能回

### 9.1 建議附的截圖（4 張）

| 截圖 | 拍哪裡 |
|---|---|
| 1 | 右上角頭像展開 → 看得到「變更密碼」 |
| 2 | 設定 → 權限管理 → 建立角色的表單（顯示「看得到誰的資料」三選一）|
| 3 | 任務看板右上「存查」按鈕 → 點進去的存查頁（含新的篩選列）|
| 4 | 素材頁的新篩選列 |

### 9.2 回覆內容

```
您好，8/24 提的 10 點都看過了，回覆如下。

【已經可以用了，只是位置不明顯 —— 這三項不用等】

1. 總經理改密碼
   點右上角您的頭像 → 選單裡的「變更密碼」。（截圖 1）
   任何登入的人都能自己改，不需要找我們。

2. 後台人員管理開權限給人資
   設定 → 權限管理 → 建立角色。（截圖 2）
   建一個叫「人資」的角色，勾選他能做的事，再到「部門/成員」把那個人的角色改成人資即可。
   建立時有一欄「看得到誰的資料」，全公司／只看自己部門／只看自己，您自己選就好。
   ▸ 只提醒一點：選「只看自己部門」的話，那位同仁要先在「部門/成員」被指定部門，
     否則他登入後會什麼都看不到。

3. 已核對的資料存在哪
   任務看板右上角有「存查」按鈕。（截圖 3）
   公告、已完成、以及您標記「不用追」的項目都收在那裡，隨時可以改回待辦。

【這次做好的】

4. 素材可以下載了，每個檔案下方都有「下載」按鈕。
5. 素材可以用「日期 + 群組」篩選了。（截圖 4）
6. 存查頁也加了同一組篩選，而且改成分頁 —— 之前只看得到最近 50 筆，現在全部翻得到。
7. 指派任務時可以同時「知會其他人」，被知會的人會收到 LINE 私訊。
   （只發個人私訊，不會在群組裡出聲）
8. 「併發限制」那類技術參數已經從您的畫面移除 —— 那是我們調整系統效能用的，
   不是您需要設定的東西，放在那裡只會造成困擾。

【一個名稱調整，先跟您說一聲】

原本的「簽核 / 已簽核」改成「核對 / 已核對」。

您提到「已簽核卻寫著卡住 24 天，看起來矛盾」—— 我們查了，兩個講的其實是不同的事：

  · 「已簽核」的原意是：**主管確認 AI 整理得對**（這件事真的存在、內容沒抽錯）
  · 「卡住 24 天」講的是：**事情本身還沒有人去做**

所以「已簽核 + 卡住 24 天」= 您確認過這件事是真的，但 24 天沒進度 —— 邏輯是通的，
是「簽核」這個詞聽起來太像「處理完了」。

改成「核對」之後，就不會跟「做完了」混在一起。畫面上的字會變，資料完全沒有變動。

【會議記錄 —— 查到的結果跟您想的不太一樣，想先跟您確認方向】

您說「有回覆所以直接進到已簽核，但這個項目還沒完成」。

我們查下來：系統其實**沒有**自動把東西變成已核對的機制，那一定要人按才會。
您看到的應該是「存查」區 —— 而它跑進存查，是因為 AI 把「有人回了『收到』」
讀成了「這件事做完了」。這一段我們已經修好，往後不會再這樣判。

但我們覺得您真正在意的可能是另一件事：

  您說的「這個項目還沒完成」，指的是**會議裡決定要做的那些事**，
  而不是「這場會議」本身，對嗎？

  現在 AI 是把「開了一場會」當成一件事，整場會議變成一筆紀錄。
  如果您要的是會議裡的每一項決議都各自變成一張待辦、各自追蹤到完成，
  那是另一種做法，我們可以做，但想先確認方向再動手。

  ▸ 麻煩您回一句：您要的是「**把會議記錄存起來可以查**」，
    還是「**會議裡決定要做的事，一項一項追到完成**」？

  （如果是後者，要先提醒您一個影響：一場會可能會抽出 5–10 項待辦，
    每天要核對的量會明顯變多，您那邊的節奏需要一起調整。）

【還有一項想跟您確認】

第 5 點「任務先給部門主管、需要才給總經理」——

  · 如果您說的是**畫面**：現在已經是這樣了。部門主管只看得到自己部門的任務，
    只有總經理室看得到全公司。
  · 如果您說的是**LINE 通知**：目前是直接通知被指派的人，沒有「先給主管、
    主管再決定要不要往上報」這一層。

  ▸ 請問您指的是哪一個？

以上，**只有兩個問題要麻煩您回**（就是上面兩個 ▸）：

  1. 會議記錄：您要的是「存起來可以查」還是「決議一項一項追到完成」？
  2. 第 5 點：您指的是畫面還是 LINE 通知？

收到之後我們接著往下做。其他的您隨時可以自己在後台調整，不用等我們。
```

---

## 10. 變更紀錄

| 日期 | 版本 | 變更 | 作者 |
|---|---|---|---|
| 2026-08-25 | **v1.0** | **SHIPPED** · M0.6／M1／M2／M3a／M3b／M4 全部落地（8 個 commit）· OQ-2 裁定「先日期＋群組」· OQ-6 查完＝不是 bug 是 prompt 太薄 · OQ-7 被推翻（加「會議記錄區」治不了根本，真正的缺口是抽取顆粒度）→ 轉 OQ-9 待問客戶 · ⭐ 途中查出兩個沒人回報過的 P0：**存查入口會隨票數增加而消失**（`counts.archived` 從 500 筆窗口數）、**拿掉 API 欄位會讓 rolling deploy 期間的舊前端白畫面** · §9 FMEA 19 條 | ahern + Claude Code |
| 2026-08-25 | v0.1 | M0 首版 · 10 項逐一對照程式碼判定 · ⭐ 3 項已可用（改密碼／人資角色／存查頁）、2 項是命名誤解（已簽核≠已完成、併發限制不該給客戶看）· ⭐ ⑨ 正好補上 custom-roles 缺的 OQ-CR-7 答案（客戶要的自訂角色＝「人資」）· ⑧ 拆成三個問題，其中 a 可能是 bug 要先查 · 8 個 OQ 待裁定，其中 4 個需問客戶 | ahern + Claude Code |
