# 設計文件 · 台灣福祉 2026-08-24 修改建議（M0）

> ✅ **M0 APPROVED**（2026-08-25）· OQ-1/3/5/7 全採建議 · **OQ-2/4/8 待問客戶** · OQ-6 進任務 #37
> 📋 原始 M0 · 待裁定 OQ-TWH-1..8 · 來源：`2026.08.24台灣福祉戰情室修改建議.docx`（10 項）
> 相關：[`media-and-vision.md`](media-and-vision.md)、[`task-completion-tracking.md`](task-completion-tracking.md)、
> [`custom-roles.md`](custom-roles.md)、[`notify-tenant-scoping.md`](notify-tenant-scoping.md)
>
> **一句話**：10 項裡 **3 項已經做好了只是客戶不知道**、**2 項是命名造成的誤解不是 bug**、
> 真正要開發的只有 5 項 —— 而其中最大的一項（⑧會議記錄）需要先問客戶才動得了。

---

## 1. 逐項判定（先分類，再談做法）

⚠️ 每一項都對照過實際程式碼，不是憑印象。判定依據列在各項下面。

| # | 需求 | 判定 | 工作量 |
|---|---|---|---|
| ① | 素材檔案可以下載 | 🟡 **後端已有，缺一顆按鈕** | XS |
| ② | 素材加搜尋（日期／關鍵字）| 🔵 要開發 | S |
| ③ | 「已簽核」又寫「卡住 24 天」矛盾 | 🟠 **不是 bug · 命名問題** | S（改文案）|
| ④ | 指派時同時通知其他人 | 🔵 要開發 | M |
| ⑤ | 任務先給部門主管、需要才給總經理 | 🟠 **多半已成立 · 待確認實際情境** | ? |
| ⑥ | 已簽核的資料存哪 + 搜尋 | 🟡 **已有「存查」頁，但有 50 筆上限、無搜尋** | S |
| ⑦ | 「併發限制」是什麼功能 | 🟠 **不是需求 · 是不該給客戶看的欄位** | XS |
| ⑧ | 會議記錄讓 AI 判讀、進「會議記錄區」 | 🔵 **最大的一項 · 需先問客戶** | L |
| ⑨ | 後台人員管理開權限給人資 | 🟢 **已經可以做了**（自訂角色 08-21 上線）| XS（教學）|
| ⑩ | 總經理要改密碼 | 🟢 **早就能改了 · 找不到入口** | XS（教學）|

> 🟢 已可用 ｜ 🟡 差最後一哩 ｜ 🟠 誤解／文案 ｜ 🔵 要開發

---

## 2. 🟢 已經可以用，但客戶不知道（⑨⑩）

### 2.1 ⑩ 總經理改密碼 —— 一直都能改

`POST /auth/change-password` 存在，`ChangePasswordDialog` 也掛在 `Shell.tsx`，
入口在**右上角頭像 → 選單 → 變更密碼**，任何登入的人都能用。

**這不是功能缺口，是找不到。** 而且它正好落在同一個病灶上：
使用者選單藏在頭像後面，沒點開不知道裡面有什麼。

> ⭐ 這一項不要寫成「開發項目」回覆客戶 —— 那會讓對方以為我們沒做過。
> 正確回法：「已經可以了，位置在這裡（附截圖）」。

### 2.2 ⑨ 人資要能管人員 —— 自訂角色 2026-08-21 已上線

「後台人員管理開權限給人資」＝ 建一個只能管成員、看不到其他東西的角色。
`custom-roles` v0.3（`dbb5996` / `9cd7263`）已經做完並上 prod：

```
設定 → 權限管理 → ＋ 建立角色
  名稱：人資
  看得到誰的資料：只看自己 / 只看自己部門 / 看全公司  ← 三選一
  可以做哪些事：勾「查看成員」「分配部門」…
再到 部門/成員 把那個人的角色改成「人資」
```

⚠️ **這正好是 OQ-CR-7 一直缺的那個答案**（「客戶到底要哪個自訂角色」）——
現在有了：**人資**。任務 #34 可以用這個跑驗收。

⚠️ 但有一個**必須先確認**的：人資要不要看得到**全公司**的人？
- 要 → 基準選「看全公司」（`tenant_admin` 基準）
- 只管自己部門 → 基準選「只看自己部門」，而且**那個人必須先有部門**（V-7）

---

## 3. 🟠 誤解與文案（③⑤⑦）

### 3.1 ⭐ ③「已簽核」又「卡住 24 天」—— 兩個不同的軸，不是矛盾

看程式碼（`warroom-tasks.service.ts:213`）：

```ts
const isStuck = (r) =>
  r.work_status === "open"          // 事情還沒做完
  && r.confirm_status === "已簽核"   // 但主管已經確認過「AI 讀對了」
  && daysSince(r.created_at) > GRACE_DAYS;
```

| 欄位 | 真正的意思 | 總經理以為的意思 |
|---|---|---|
| `confirm_status = 已簽核` | **確認 AI 整理得對**（這件事真的存在、內容沒抽錯）| ❌「這件事處理完了」|
| `work_status = open` | 事情**還沒做完** | — |

**所以「已簽核 + 卡住 24 天」＝「你確認過這件事是真的，但 24 天沒人去做」——
邏輯完全一致，是「簽核」這個詞被誤讀。**

⭐ 這跟 `recordStatusLabel.ts` 早就記過的問題同源：
> `resolved` 刻意**不叫「已完成」**：那是 AI 讀到的（推論），而「完成」是本人回報的（承諾）。
> 兩者用同一個詞的話，看板上會出現「已完成但尚未確認完成」這種自相矛盾的句子。

**當時把這條紀律套在 `records` 上，卻沒套在 `tickets` 的「簽核」上。**

**建議改法（純文案，不動資料）**：

| 現在 | 改成 |
|---|---|
| 已簽核 | **已確認**（＋ tooltip：「主管確認過 AI 整理得對；事情做完沒有另外看『進度』」）|
| 卡住 24 天 | **確認後 24 天沒進度** |
| 待簽核 | **待確認** |

### 3.2 ⑤ 任務先給部門主管、需要才給總經理

**資料層本來就是這樣**：`tickets` 的 RLS 讓 `group_owner` 只看得到自己部門的任務，
`tenant_admin` 才看全公司。所以「總經理看到全部」是**他的角色本來的設計**，不是漏擋。

⚠️ 但客戶說的可能是**通知**不是**畫面** —— 現在還不確定他指哪一個：
- **(a) 畫面**：已經是分層的，不用改（要說明給他聽）
- **(b) LINE 通知**：目前指派通知只發給**被指派的那個人**（`assign-notify.service.ts`），
  沒有「先通知主管、主管決定要不要往上報」這條鏈
- **(c) 總經理不想看到全部**：那是他自己的角色範圍太寬 —— 可以用自訂角色做一個
  「只看摘要不看細節」的視角，但這是產品方向不是 bug

**這一項不寫需求，先問清楚**（OQ-TWH-4）。

### 3.3 ⑦「併發限制」是什麼 —— 客戶不該看到這個欄位

`concurrency` 是**分析批次同時跑幾個群組**的技術參數，屬成本控管。
`scheduler-config.service.ts:11` 的註解自己就寫了：

> 欄位級 whitelist · **tenant_admin 不能改**成本控管欄位（concurrency / lookback_days）

**改不了卻看得到 —— 那就是純粹的困惑來源。** 客戶會問「這是什麼」正是這個原因。

**建議**：租戶端**不顯示** `concurrency` 與 `lookback_days`（aiproot 端保留）。
這跟 08-21 那個「助理角色看得到、改不動、也用不了」是同一類。

---

## 4. 🟡 差最後一哩（①⑥）

### 4.1 ① 素材下載 —— 後端已經有了

`GET /media/:mediaId/content`（`media:view`）已存在，客戶的記憶是對的。
它現在回 `content-disposition: inline`（給縮圖與預覽用）。

**要做的**：
- 前端加「下載」按鈕
- 下載時要 `attachment` 而不是 `inline` → 加一個 `?download=1` 參數切換 disposition
- 中文檔名已經走 RFC 5987，不用另外處理

⚠️ **不要**用前端 `<a download>` 硬幹：跨來源時瀏覽器會忽略 `download` 屬性，
變成直接開新分頁。要在後端下對 header。

### 4.2 ⑥ 已簽核的資料存哪 + 搜尋

**存在「存查」頁**（任務看板右上「存查」按鈕）—— 客戶可能沒發現。
但目前有兩個實際限制：

```ts
const archived = notTracked.slice(0, 50);   // warroom-tasks.service.ts:296
```

- **只回 50 筆**，沒有分頁 → 累積久了看不到舊的
- **沒有搜尋**

**建議**：存查頁加分頁 ＋ 關鍵字搜尋（跟 ② 的素材搜尋一起做，兩者形狀一樣）。

---

## 5. 🔵 要開發（②④⑧）

### 5.1 ② 素材搜尋（日期／關鍵字）

現況只有「類型」篩選（`MediaLibrary.tsx:79`）。要加：
- 日期範圍
- 關鍵字（比對什麼？**檔名？群組名？訊息內容？** → OQ-TWH-2）

### 5.2 ④ 指派時同時通知其他人

現況 `assign-notify.service.ts` 只通知**被指派的那一位**。

要決定的是**通知誰**：
- (a) 固定加上該部門主管
- (b) 指派時可以自己勾要一起通知的人
- (c) 通知到 LINE 群組並 @ 那些人 ← 這條**已經有 M0**：[`notify-group-mention.md`](notify-group-mention.md)

⚠️ **注意 memory `project_line_group_never_speaks`**：2026-08-12 已裁定
**LINE 群組一律不出聲**（reply 是全群可見）。所以 (c) 要走**主動 push 到群**，
不是 reply，而且要重新確認那條裁定涵不涵蓋這種情境（OQ-TWH-5）。

### 5.3 ⑧ ⭐ 會議記錄 —— 最大的一項，且**不能直接開工**

客戶的原話：
> 他們有一整坨的會議記錄，可能有回覆所以直接進到已簽核，但這個項目還沒完成。
> 不知道會議記錄這個部分能否讓 AI 判讀並且進到會議記錄區之類的

拆開來其實是**三個不同的問題**：

| | 問題 | 性質 |
|---|---|---|
| a | 「有回覆就直接進已簽核」 | ⚠️ **可能是 bug** —— 要查完成訊號的判定是不是太寬鬆 |
| b | 「這個項目還沒完成」 | 同 ③ —— 已簽核 ≠ 已完成，可能又是命名誤解 |
| c | 「進到會議記錄區」 | 🔵 新功能：會議記錄要獨立成一個區域／分類 |

⚠️ **c 有一個現成的機制被忽略了**：AI 已經可以自己造分類，而 08-24 剛修好
「新分類必須用繁體中文」（`a477a61`）。所以「會議記錄」**可能不需要寫程式** ——
它可以是一個分類，在「任務設定 → 分類詞庫」裡管理。

但要先確認 a：**如果會議記錄是因為 bug 才跑進已簽核，那先修 bug，不是加新區域。**

---

#### 5.3.1 a 的查核結果（2026-08-25 · 任務 #37）

**結論：不是 bug，但客戶的直覺指到了一個真的缺口。**

| 客戶說的 | 實際上 |
|---|---|
| 「直接進到**已簽核**」 | `laneFor()` **從來不會回已簽核** —— 材料化只產生 待簽核／待確認／存查。已簽核只有人按下去才有 |
| 客戶看到的其實是 | **存查**（`high` + `resolved` → 存查），而存查頁副標寫著「公告 / 已完成 / 已忽略 · **不需核對的紀錄**」，讀起來就是做完了 |
| 「有回覆」導致的 | ✅ 這一段是真的 —— 但問題在 **prompt**，不在判定邏輯 |

**根因**：`tenant-twh.ts` 抽取規則 5 原本只有一行、沒有任何限定：

```
5. status：open = 尚未處理、in_progress = 處理中、resolved = 已解決、info = 純資訊/知識。
```

會議記錄裡有人回「收到」，模型判成 `resolved` 完全講得通 ——
**「訊息被回應了」被讀成「事情做完了」**。這跟 08-24 分類中英混雜是同一類：
prompt 太薄，模型用一個講得通但不對的讀法填空。

**已修（M0.6）**：規則 5 補上限定 ——「有人回覆不等於事情做完」、列出實際會出現的
回覆詞、明講「會議開完了」不算 resolved、以及**判斷不了就填 open**（兩邊錯誤成本
不對稱：誤填 open 是多一次 30 秒核對，誤填 resolved 是那件事沒人再看到）。
`server/test/status-resolved-semantics.test.ts` 釘住這幾句不被後續改 prompt 弄丟。

**順帶查到的好消息**：`resolved` **不會**讓卡片離開工作生命週期 ——
`workStatusFor()` 只擋紀錄類分類與 `info`（de8d291 保守版的裁定）。
所以誤判 resolved 的票在當責人的待辦清單上還在，只是簽核分區顯示成「存查」。
**這次只需要修 prompt，不用動 work_status。**

#### 5.3.2 ⭐ 但真正的缺口比 a 深 —— 顆粒度，不是狀態

客戶說「**這個項目還沒完成**」。他要的不是那筆會議記錄的狀態，
是**會議裡決定要做的事**。

現在 prompt 的規則 1 是「一事一筆」，而 AI 把「開了一場會」當成那個「事」：

| | 現況 | 客戶要的 |
|---|---|---|
| 一場會議 | 抽成 **1 筆**「會議記錄」→ 存查 | 抽出**每一項決議** → 各自成為一張待辦卡 |

**這不是狀態判錯，是抽取顆粒度不對。**
加一個「會議記錄區」治不了它 —— 那只是把同一筆東西換個地方放。

⚠️ **動之前必須確認的**：`templates.ts` 規則 14 已經在處理「線上週會議／部門週會」
這類項目 —— 但那是**報工單（service_reports）**的 items 規則，跟這裡的 `records`
是不同 schema。改 records 的顆粒度不會直接打到日報抽取，但**要一起回歸驗證**（R12）。

⚠️ 另一個風險：一場會抽 5–10 筆決議，**待核對佇列的量會跳一個級距**。
主管每天 3 分鐘的核對節奏可能被打破 —— 這是要跟客戶講清楚的取捨，不是實作細節。

---

## 6. 開放問題（OQ-TWH-N）

| # | 問題 | 建議 |
|---|---|---|
| **OQ-TWH-1** | ③ 的「簽核」要不要改名成「確認」？ | ✅ **裁定：全站改名** · 含手冊、LINE 通知文案、簽核率指標 · **一次改完**（改一半就是 0069 那個教訓）|
| **OQ-TWH-2** | ② 素材搜尋的關鍵字比對什麼？檔名／群組名／該則訊息內容？ | ✅ **裁定（2026-08-25 用戶）：先做日期＋群組兩個篩選** · 是**篩選**不是全文檢索，不碰內容比對 · 已實作 `be42ab7` `0051af5` |
| **OQ-TWH-3** | ⑦ 租戶端要不要直接隱藏 concurrency / lookback_days？ | ✅ **裁定：隱藏** · aiproot 端保留 |
| **OQ-TWH-4** | ⑤ 客戶指的是**畫面**還是**通知**？ | **需問客戶** · 畫面已經分層了，通知沒有 |
| **OQ-TWH-5** | ④ 要不要通知到 LINE 群？ | ✅ **裁定：只發個人私訊** · 指派時可勾額外要通知的人，各自收到 LINE 私訊 · **不碰群組** —— 2026-08-12「LINE 群組一律不出聲」的裁定維持不變 |
| **OQ-TWH-6** | ⑧a 會議記錄「因為有回覆就進已簽核」是不是 bug？ | ✅ **已查（#37 · §5.3.1）：不是 bug** · 沒有自動進已簽核的路徑；客戶看到的是「存查」· 根因是 prompt 對 `resolved` 沒有限定 → **M0.6 已修** |
| **OQ-TWH-7** | ⑧c「會議記錄區」是新分類就夠，還是要獨立頁面？ | ⏸ **兩者都不對** · 查下來真正的缺口是**抽取顆粒度**（§5.3.2）—— 會議該抽出「每一項決議」而不是「開了一場會」。加區域／加分類都只是換位置擺。→ 轉 **OQ-TWH-9** |
| **OQ-TWH-9** 🆕 | 客戶要的是**會議記錄本身**（存起來可查），還是**會議裡的待辦**（各自追蹤到完成）？ | **需問客戶** —— 決定要不要改抽取顆粒度。若是後者，待核對佇列的量會跳一個級距，要先講清楚取捨 |
| **OQ-TWH-8** | ⑨ 人資角色要看**全公司**還是**只有自己部門**的人？ | **需問客戶** · 決定基準選哪個 · 選「只看自己部門」的話那個人要先有部門 |

---

## 7. 里程碑

| # | 內容 | 依賴 | 估 |
|---|---|---|---|
| **M0** 📋 | 本文 · 待裁定 OQ-TWH-1..8 | — | ✅ |
| **M0.5** | ⭐ **先回客戶「這 3 項已經可以用了」**（⑨⑩＋⑥的存查頁）· 附操作位置截圖 | — | 立刻 |
| **M0.6** | ✅ 查 ⑧a ＋ 修 prompt 對 `resolved` 的限定（§5.3.1）| OQ-6 | ✅ |
| **M1** | ✅ ⑦ 租戶端隱藏成本控管欄位 ＋ ① 素材下載按鈕（`7d32d02`）| OQ-3 | ✅ |
| **M2** | ✅ ③ 簽核→核對 全站改名（含手冊）（`bbe74d8` `bef74e6`）| OQ-1 | ✅ |
| **M3a** | ✅ ② 素材依日期＋群組篩選（`be42ab7` `0051af5`）| OQ-2 | ✅ |
| **M3b** | ⏸ ⑥ 存查分頁與搜尋 · 沿用 M3a 的篩選形狀 | — | S |
| **M4** | ✅ ④ 指派通知擴充 · 只發個人私訊（`d8482da` `4bca96b`）| OQ-5 | ✅ |
| **M5** | ⏸ ⑧ 會議決議抽取顆粒度（**不是**加「會議記錄區」）· 需回歸驗證 R12 | **OQ-9**（未問）| L |

⚠️ **M0.5 排第一** —— 有 3 項客戶以為缺的東西其實早就有了。
先回這個，客戶對進度的感受會完全不同，而且成本是零。

### 7.1 目前卡在哪（2026-08-25）

程式碼這邊做得完的**都做完了**。剩下 3 項全部卡在同一個地方：**問客戶**。

| 卡住的 | 要問什麼 | 為什麼不能猜 |
|---|---|---|
| ⑤ | **OQ-TWH-4**：指的是**畫面**還是**通知**？ | 畫面已經分層了；通知沒有。搞錯方向等於做了不需要的東西 |
| M5（⑧）| **OQ-TWH-9**：要**會議記錄**還是**會議裡的待辦**？ | 決定要不要改抽取顆粒度 —— 影響待核對佇列的量級 |

M3b（⑥ 存查分頁與搜尋）**不卡人** —— 沿用 M3a 已經做好的日期＋群組篩選形狀就好。

＋ **OQ-TWH-8**（⑨人資角色看全公司還是只有自己部門）不擋開發，但擋驗收。

⚠️ M1／M2／M4 已 commit 但**尚未 push** —— 客戶那邊還看不到。

---

## 8. 變更紀錄

| 日期 | 版本 | 變更 | 作者 |
|---|---|---|---|
| 2026-08-25 | v0.1 | M0 首版 · 10 項逐一對照程式碼判定 · ⭐ 3 項已可用（改密碼／人資角色／存查頁）、2 項是命名誤解（已簽核≠已完成、併發限制不該給客戶看）· ⭐ ⑨ 正好補上 custom-roles 缺的 OQ-CR-7 答案（客戶要的自訂角色＝「人資」）· ⑧ 拆成三個問題，其中 a 可能是 bug 要先查 · 8 個 OQ 待裁定，其中 4 個需問客戶 | ahern + Claude Code |
