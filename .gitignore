node_modules/
output/
dist/
.DS_Store

# ⚠️ 憑證 · `.env` 這個 pattern **只比對確切檔名**，不會蓋到 .env.prod / .env.local。
#    少了下面那行，新增的 .env.prod 會直接被 commit 上去。
.env
.env.*
!.env.example

# 內含真名/PII 與原始 prototype — 絕不進版控（更不可推上 Render 拉取的 repo）
合夥人/

# Ragic 全量匯出（客戶原始資料，禁 commit）
.ragic-export/
.env.debug

# SOP 靜態站的建置產物（Render build command 產生 · 不進版控）
sop-site/

# i18n 掃描產物
web/output/

# 拋棄式量測腳本
web/scripts/_*.tmp.mjs

# ── 文件不進 GitHub（2026-08-31 裁定）────────────────────────
# ⚠️ 這個 repo 是 **public**。設計文件、市場研究、客戶 SOP 都不該公開。
#    例外只留 CLAUDE.md / AGENTS.md —— 那兩份是給 AI 助理讀的專案規則，
#    不是文件；少了它們，換一台機器 clone 下來就沒有任何開發紀律可循。
# ⚠️⚠️ 這只擋「以後」。已經 push 過的仍然留在 git 歷史裡，公開可讀。
*.md
!CLAUDE.md
!AGENTS.md
