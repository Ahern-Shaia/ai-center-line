# Render Static Site rewrite rules
# 動之前先把 BACKEND_URL 換成 Render 上 backend service 的實際網址（如 https://aiproot-warroom-api.onrender.com）

# 前端 /api/* 代理到 backend service
/api/*  https://BACKEND_URL/:splat  200

# SPA fallback：所有未匹配路徑打回 index.html（React 內部 route）
/*      /index.html                200
