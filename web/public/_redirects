# Render Static Site rewrite rules

# SPA fallback：所有未匹配路徑打回 index.html（React 內部 route）
# 注意：/api/* 由前端直打 backend URL（VITE_API_BASE_URL 環境變數控制）
# Render Static Site 對 POST 的 rewrite 不可靠，所以不走 rewrite proxy
/*  /index.html  200
